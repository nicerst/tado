interface:
  display_name: "Context7"
  short_description: "Fetch live library docs into context"
  default_prompt: "Use $context7 to fetch current library documentation before implementing this change."

policy:
  allow_implicit_invocation: true
